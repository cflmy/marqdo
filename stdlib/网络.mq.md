---
title: lib/网络 — HTTP
description: HTTP(S) 与地址编码
> lib/网络.mq.md
---

# main

导入 lib/网络.mq.md。函数：获取(地址)、提交(地址, 内容；可选 content_type / headers)、编码地址(内容)。支持 HTTP 与 HTTPS（ureq）。

*`e` = > 网络.编码地址 内容=你好 *

> 打印 内容=`e`
