---
title: 行向记录表
description: 首列表头「行」→ 每行一个字典
---

# main

首列表头为 `行`（或 `@` / `row`）时，每行数据是一个字典；标记列不进入字段。嵌套取元：`[字段]([1](订单))`。

`订单` =

| 行 | 品名 | 数量 |
|----|------|------|
| 1 | 苹果 | 2 |
| 2 | 梨 | 3 |

**`名` = [品名]([1](订单))**

> 打印 内容=`名`

**`量` = [数量]([2](订单))**

> 打印 内容=`量`
