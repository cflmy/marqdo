---
title: lib/系统 — 进程
description: 环境变量、dotenv、工作目录、参数、退出、执行
> lib/系统.mq.md
---

# main

导入 lib/系统.mq.md。函数：取环境、设环境、加载环境(可选路径)、参数表、工作目录、退出(码)、执行(命令)。加载环境不覆盖已有变量。view 下退出会软失败，不结束进程。

*`d` = > 工作目录 *

*`n` = > 长度 值=`d` *

+ `n` > 0
  > 打印 内容=工作目录就绪
+ *
  > 打印 内容=工作目录空
