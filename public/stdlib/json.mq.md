---
title: lib/json — JSON
description: Shared EN/ZH path; parse and stringify
> lib/json.mq.md
---

# main

Import lib/json.mq.md from both English and Chinese docs (no translated twin). Functions: parse(text), stringify(value), get(value, key), keys(value). Objects become the map runtime type.

*`obj` = > parse text={} *

*`ty` = > type `obj` *

> print text=`ty`

*`out` = > stringify value=`obj` *

> print text=`out`
