---
title: 中层标准库 — 正则与编解码
description: lib/正则 + lib/编码，无需插件（中层 M1）
导入 正则:lib/正则.mq.md
导入 编码:lib/编码.mq.md
---

# 标准库中的正则与编解码

中层标准库：日常原语进 `lib/`，不必 ABI 插件。

# main

*`ok` = > 正则.匹配 内容="user@example.com" 模式="^[^@]+@[^@]+$"*
*`tok` = > 编码.基64编码 内容="marqdo"*
*`hex` = > 编码.十六进制编码 内容="ok"*
> 打印 内容=`ok`
> 打印 内容=`tok`
> 打印 内容=`hex`
1. `ok`
  > 打印 内容=mid-m1-ok
2. *
  > 打印 内容=mid-m1-fail
