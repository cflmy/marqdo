---
title: 中层标准库 — 配置表、超文本、基32
description: TOML 配置 + HTML 转义 + base32（Mid2 M8）
导入 配置:lib/配置表.mq.md
导入 超:lib/超文本.mq.md
导入 编:lib/编码.mq.md
---

# 无需插件的配置与安全字面

# main

*cfg = > 配置.解析 内容="app = \"演示\"\n"*
> print text=`cfg`[^app]

*safe = > 超.转义 内容="<tag>"*
> print text=`safe`

*b = > 编.基32编码 内容="ok"*
> print text=`b`
