//! Recursive-descent parser → AST (no Bison).
//! Implementation arrives in M1–M2.

#![allow(dead_code)]

#[derive(Debug, Default)]
pub struct Module {
    // Filled by parser in later milestones.
}
