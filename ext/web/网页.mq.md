---
title: ext/web/网页
description: 官方网站类（中文）。表格 + 方法；不写袋式胶水。
> lib/plugin.mq.md
> lib/sys.mq.md
> lib/json.mq.md
---

## 确保插件

加载 ABI v2 `web` 插件。

*`p` = > plugin.native_path name=web *
1. `p`
  > plugin.load path=`p`
2. *
  > print text=ext/web: 未找到原生 web 插件（请 build marqdo_plugin_web 或 marqdo ext add web）
  > sys.exit code=1
****

# 页面
    + `标题`="站点"
    + `引言`=""

> 确保插件
**> web_page_new title=`标题` intro=`引言`**

## 组件装配
    + `组件`

**> web_compose_components page=`self` components=`组件`**

## 主体装配
    + `主体`

**> web_compose_main page=`self` main=`主体`**

## 渲染
    + `数据库`=None

1. `数据库`
  *`url` = > json.get value=`数据库` key=url *
  **> web_render page=`self` url=`url`**
2. *
  **> web_render page=`self`**

# 样式
    + `名`=""

**`self`**

## 样式处理
    + `样式`=None
    + `名`=None
    + `路径`=None

**`self`**

# 数据库
    + `地址`="sqlite:site.db"

> 确保插件
**> web_db_new url=`地址`**

## 初始化
    + `名`
    + `字段`

*`url` = > json.get value=`self` key=url *
**> web_db_init url=`url` name=`名` fields=`字段`**

## 插入
    + `表`
    + `行`

*`url` = > json.get value=`self` key=url *
**> web_db_insert url=`url` table=`表` rows=`行`**

## 查询
    + `表`
    + `条件`=None
    + `上限`=200

*`url` = > json.get value=`self` key=url *
*`r` = > web_db_select url=`url` table=`表` limit=`上限` *
**> json.get value=`r` key=rows**

## 获取
    + `表`
    + `id`

*`url` = > json.get value=`self` key=url *
**> web_db_get url=`url` table=`表` id=`id`**

## 更新
    + `表`
    + `id`
    + `行`

*`url` = > json.get value=`self` key=url *
**> web_db_update url=`url` table=`表` id=`id` row=`行`**

## 删除
    + `表`
    + `id`

*`url` = > json.get value=`self` key=url *
**> web_db_delete url=`url` table=`表` id=`id`**

## 执行
    + `sql`
    + `参数`=None

*`url` = > json.get value=`self` key=url *
**> web_db_exec url=`url` sql=`sql` args=`参数`**

# 表单
    + `表`=None
    + `动作`=插入
    + `id`=None

字段表 + 校验表；提交经 `# 数据库` 写入。

> 确保插件
**> web_form_new table=`表` action=`动作` id=`id`**

## 字段
    + `字段`

**> web_form_fields form=`self` fields=`字段`**

## 规则
    + `规则`

**> web_form_rules form=`self` rules=`规则`**

## 校验
    + `规则`=None
    + `数据`

**> web_form_validate form=`self` rules=`规则` data=`数据`**

## 渲染
    + `id`=form
    + `数据`=None
    + `错误`=None

**> web_form_render form=`self` id=`id` data=`数据` errors=`错误`**

## 提交
    + `数据`
    + `数据库`

*`url` = > json.get value=`数据库` key=url *
**> web_form_submit form=`self` data=`数据` url=`url`**

# 应用
    + `页面`
    + `数据库`=None
    + `后台`=False
    + `主机`=127.0.0.1
    + `端口`=18081

> 确保插件
**> web_app_new page=`页面` db=`数据库` admin=`后台` host=`主机` port=`端口`**

## 路由
    + `路径`
    + `页面`

将已装配页面挂到 `路径`（如 `/about`）。`/` 由首页 `页面=` 提供。

**> web_app_route app=`self` path=`路径` page=`页面`**

## 挂载表单
    + `id`
    + `表单`

**> web_app_mount_form app=`self` id=`id` form=`表单`**

## 监听

**> web_listen app=`self`**
