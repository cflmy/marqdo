---
title: ext/ai/大模型
description: >-
  Marqdo 大模型智能原语 — 提问 / 流式 / 收集。
  OpenAI 兼容 HTTP 仅为内部后端，不是编排表面。
导入 系统:lib/系统.mq.md
导入 网络:lib/网络.mq.md
import json:lib/json.mq.md
导入 表:lib/表.mq.md
---

## 加载环境
    + `路径`=None

从工作目录加载 `.env`（可选 `路径=`）。不覆盖已有变量。

*> 系统.加载环境 路径=`路径`*

## 收集
    + `事件`

把流式事件列表收成最终答复文本。兼容别名：`流式结果`。

**answer = ""**

- [ev](事件)
  **t = [type](ev)**
  1. `t` == "done"
    **answer = [result](ev)**
  2. `t` == "error"
    **answer = [message](ev)**
  3. `t` == "delta"
  4. `t` == "text"
    **chunk = [text](ev)**
    1. `chunk`
      **answer = answer + chunk**
    2. *
      **_ = 1**
  5. *
    **_ = 1**

*answer*

## 流式结果
    + `事件`

`收集` 的兼容别名。

*> 收集 事件=`事件`*

## 创建
    + `模型`=None
    + `基址`=None
    + `密钥`=None
    + `后端`=openai-compatible

工厂：构造大模型句柄（同 `# 大模型` 构造）。

**h = > 大模型 模型=`模型` 基址=`基址` 密钥=`密钥` 后端=`后端`**
*h*

## 提问
    + `提示`
    + `模型`=None
    + `基址`=None
    + `密钥`=None

模块便捷入口：创建默认句柄并返回**答复文本**。

**m = > 创建 模型=`模型` 基址=`基址` 密钥=`密钥`**
**r = > m.提问 提示=`提示`**
*[text](r)*

# 大模型
    + `模型`=None
    + `基址`=None
    + `密钥`=None
    + `后端`=openai-compatible

大模型句柄 — 智能原语，不是 HTTP 客户端。

1. `密钥`
  **key = 密钥**
2. *
  **key = > 系统.取环境 名="OPENAI_API_KEY"**
  1. 非 `key`
    **key = > 系统.取环境 名="MARQDO_LLM_API_KEY"**
  2. *
    **_ = 1**

1. 非 `key`
  > 打印 内容=ext/ai/大模型: 请设置 OPENAI_API_KEY 或 MARQDO_LLM_API_KEY（或传入 密钥=）
  > 系统.退出 码=1
2. *
  **_ = 1**

1. `基址`
  **url = 基址**
2. *
  **url = > 系统.取环境 名="OPENAI_BASE_URL"**
  1. 非 `url`
    **url = > 系统.取环境 名="MARQDO_LLM_BASE_URL"**
  2. *
    **_ = 1**
  1. 非 `url`
    **url = "https://api.openai.com/v1"**
  2. *
    **_ = 1**

1. `模型`
  **mdl = 模型**
2. *
  **mdl = > 系统.取环境 名="OPENAI_MODEL"**
  1. 非 `mdl`
    **mdl = > 系统.取环境 名="MARQDO_LLM_MODEL"**
  2. *
    **_ = 1**
  1. 非 `mdl`
    **mdl = "gpt-4o-mini"**
  2. *
    **_ = 1**

`h` =

| _type | backend | api_key | base_url | model | suffix | bearer |
|-------|---------|---------|----------|-------|--------|--------|
| 大模型 | `后端` | `key` | `url` | `mdl` | /chat/completions | "Bearer " |

*h*

## 提问
    + `提示`

语义提问：返回 **结果 map**（`text` / `model` / `finish` / `backend`）。

**text = > 自.运行 提示=`提示` 流式=False 打印增量=False**

`result` =

| text | model | finish | backend |
|------|-------|--------|---------|
| `text` | [model](自) | stop | [backend](自) |

*result*

## 流式
    + `提示`
    + `打印增量`=False

语义流式：返回事件列表（再用 `收集` 收成文本）。

*> 自.运行 提示=`提示` 流式=True 打印增量=`打印增量`*

## 运行
    + `提示`
    + `流式`=False
    + `打印增量`=False

内部 / 兼容原语。新代码优先 `提问` / `流式`。

**url = [base_url](自) + [suffix](自)**
**auth = [bearer](自) + [api_key](自)**
**headers = > 表.改 入=None 于="Authorization" 值=`auth`**

`messages` =

| @ | role | content |
|---|------|---------|
| 1 | user | `提示` |

`req` =

| model | messages |
|-------|----------|
| [model](自) | `messages` |

1. `流式`
  **req = > 表.改 入=`req` 于="stream" 值=True**
  **body = > json.stringify value=`req`**
  **resp = > 网络.提交流式 地址=`url` 内容=`body` headers=`headers` 打印增量=`打印增量`**
  1. [status](resp) == 200
    *[events](resp)*
  2. *
    > 打印 内容=ext/ai/大模型: HTTP 错误（流式）
    > 打印 内容=[status](resp)
    > 系统.退出 码=1
2. *
  **body = > json.stringify value=`req`**
  **resp = > 网络.提交 地址=`url` 内容=`body` headers=`headers`**
  1. [status](resp) == 200
    **data = > json.parse text=[body](resp)**
    *[content]([message]([1]([choices](data))))*
  2. *
    > 打印 内容=ext/ai/大模型: HTTP 错误
    > 打印 内容=[status](resp)
    > 打印 内容=[body](resp)
    > 系统.退出 码=1

## 聊天
    + `提示`
    + `流式`=False
    + `打印增量`=False

`运行` 的别名。

*> 自.运行 提示=`提示` 流式=`流式` 打印增量=`打印增量`*
