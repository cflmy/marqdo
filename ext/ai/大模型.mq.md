---
title: ext/ai/大模型
description: >-
  Marqdo 大模型智能原语 — 提问 / 流式 / 收集 / 命名句柄。
  OpenAI 兼容与 Ollama HTTP 藏在语义面之后。
导入 系统:lib/系统.mq.md
import json:lib/json.mq.md
导入 文件:lib/文件.mq.md
导入 文本:lib/文本.mq.md
import openai:ext/ai/llm/openai.mq.md
---

## 加载环境
    + `路径`=None

*> 系统.加载环境 路径=`路径`*

## 收集
    + `事件`

**answer = ""**

- [ev](事件)
  **t = [type](ev)**
  1. `t` == "done"
    **answer = [result](ev)**
  2. `t` == "error"
    **answer = [message](ev)**
  3. `t` == "delta"
  4. `t` == "text"
    **chunk = [text](ev)**
    1. `chunk`
      **answer = answer + chunk**
    2. *
      **_ = 1**
  5. *
    **_ = 1**

*answer*

## 流式结果
    + `事件`

*> 收集 事件=`事件`*

## 用量
    + `数据`

*> openai.usage_from data=`数据`*

## 创建
    + `模型`=None
    + `基址`=None
    + `密钥`=None
    + `后端`=openai-compatible
    + `名`=None

**h = > 大模型 模型=`模型` 基址=`基址` 密钥=`密钥` 后端=`后端` 名=`名`**
*h*

## 快速
    + `模型`=None
    + `基址`=None
    + `密钥`=None

1. `模型`
  **mdl = 模型**
2. *
  **mdl = > 系统.取环境 名="MARQDO_LLM_FAST_MODEL"**

*> 创建 模型=`mdl` 基址=`基址` 密钥=`密钥` 名="fast" 后端="openai-compatible"*

## 推理
    + `模型`=None
    + `基址`=None
    + `密钥`=None

1. `模型`
  **mdl = 模型**
2. *
  **mdl = > 系统.取环境 名="MARQDO_LLM_REASONING_MODEL"**

*> 创建 模型=`mdl` 基址=`基址` 密钥=`密钥` 名="reasoning" 后端="openai-compatible"*

## 提示正文
    + `源`

**sw = > 文本.开头是 内容=`源` 前缀="---"**
1. 非 `sw`
  *源*
2. *
  **parts = > split value=`源` sep="\n---\n"**
  **n = > len value=`parts`**
  1. `n` < 2
    *源*
  2. *
    **fm = > at value=`parts` index=0**
    **is_p = > 文本.包含 内容=`fm` 子串="type: prompt"**
    1. 非 `is_p`
      **is_p = > 文本.包含 内容=`fm` 子串="类型: 提示"**
    2. *
      **_ = 1**
    1. `is_p`
      **body = > at value=`parts` index=1**
      *> 文本.去空白 s=`body`*
    2. *
      *源*

## 加载提示
    + `路径`

**raw = > 文件.读文本 路径=`路径`**
*> 提示正文 源=`raw`*

## 提示
    + `路径`=None
    + `文本`=None

1. `路径`
  *> 加载提示 路径=`路径`*
2. `文本`
  *> 提示正文 源=`文本`*
3. *
  > 打印 内容=ext/ai/大模型.提示: 请传 路径= 或 文本=
  > 系统.退出 码=1

## 提问
    + `提示`=None
    + `路径`=None
    + `模型`=None
    + `基址`=None
    + `密钥`=None

1. `路径`
  **p = > 加载提示 路径=`路径`**
2. `提示`
  **p = 提示**
3. *
  > 打印 内容=ext/ai/大模型.提问: 请传 提示= 或 路径=
  > 系统.退出 码=1

**m = > 创建 模型=`模型` 基址=`基址` 密钥=`密钥`**
**r = > m.提问 提示=`p`**
*[text](r)*

# 大模型
    + `模型`=None
    + `基址`=None
    + `密钥`=None
    + `后端`=openai-compatible
    + `名`=None

1. `密钥`
  **key = 密钥**
2. *
  **key = > 系统.取环境 名="OPENAI_API_KEY"**
  1. 非 `key`
    **key = > 系统.取环境 名="MARQDO_LLM_API_KEY"**
  2. *
    **_ = 1**

1. `后端` == ollama
  1. 非 `key`
    **key = "ollama"**
  2. *
    **_ = 1**
2. 非 `key`
  > 打印 内容=ext/ai/大模型: 请设置 OPENAI_API_KEY 或 MARQDO_LLM_API_KEY
  > 系统.退出 码=1
3. *
  **_ = 1**

1. `基址`
  **url = 基址**
2. `后端` == ollama
  **url = > 系统.取环境 名="OLLAMA_HOST"**
  1. 非 `url`
    **url = "http://127.0.0.1:11434/v1"**
  2. *
    **_ = 1**
3. *
  **url = > 系统.取环境 名="OPENAI_BASE_URL"**
  1. 非 `url`
    **url = > 系统.取环境 名="MARQDO_LLM_BASE_URL"**
  2. *
    **_ = 1**
  1. 非 `url`
    **url = "https://api.openai.com/v1"**
  2. *
    **_ = 1**

1. `模型`
  **mdl = 模型**
2. *
  **mdl = > 系统.取环境 名="OPENAI_MODEL"**
  1. 非 `mdl`
    **mdl = > 系统.取环境 名="MARQDO_LLM_MODEL"**
  2. *
    **_ = 1**
  1. 非 `mdl`
    1. `后端` == ollama
      **mdl = "llama3.2"**
    2. *
      **mdl = "gpt-4o-mini"**
  2. *
    **_ = 1**

1. `名`
  **nm = 名**
2. *
  **nm = None**

`h` =

| _type | backend | api_key | base_url | model | suffix | bearer | name |
|-------|---------|---------|----------|-------|--------|--------|------|
| 大模型 | `后端` | `key` | `url` | `mdl` | /chat/completions | "Bearer " | `nm` |

*h*

## 提问
    + `提示`=None
    + `路径`=None

1. `路径`
  **p = > 加载提示 路径=`路径`**
2. `提示`
  **p = 提示**
3. *
  > 打印 内容=ext/ai/大模型: 提问需要 提示= 或 路径=
  > 系统.退出 码=1

**pack = > 自.完整结果 提示=`p` 流式=False 打印增量=False**
**text = > json.get value=`pack` key="text"**
**usage = > json.get value=`pack` key="usage"**
**finish = > json.get value=`pack` key="finish"**

`result` =

| text | model | finish | backend | usage | name | llm_calls | tokens |
|------|-------|--------|---------|-------|------|-----------|--------|
| `text` | [model](自) | `finish` | [backend](自) | `usage` | [name](自) | 1 | [total_tokens](usage) |

*result*

## 流式
    + `提示`=None
    + `路径`=None
    + `打印增量`=False

1. `路径`
  **p = > 加载提示 路径=`路径`**
2. `提示`
  **p = 提示**
3. *
  > 打印 内容=ext/ai/大模型: 流式需要 提示= 或 路径=
  > 系统.退出 码=1

*> 自.运行 提示=`p` 流式=True 打印增量=`打印增量`*

## 完整结果
    + `提示`
    + `流式`=False
    + `打印增量`=False

*> openai.chat_completions base_url=[base_url](自) api_key=[api_key](自) model=[model](自) prompt=`提示` stream=`流式` echo=`打印增量` suffix=[suffix](自) bearer=[bearer](自)*

## 运行
    + `提示`
    + `流式`=False
    + `打印增量`=False

1. `流式`
  **pack = > 自.完整结果 提示=`提示` 流式=True 打印增量=`打印增量`**
  *[events](pack)*
2. *
  **pack = > 自.完整结果 提示=`提示` 流式=False 打印增量=`打印增量`**
  *[text](pack)*

## 聊天
    + `提示`
    + `流式`=False
    + `打印增量`=False

*> 自.运行 提示=`提示` 流式=`流式` 打印增量=`打印增量`*
