---
title: ext/linalg/线性代数
description: 公式优先的线性代数（中文）。矩阵表达式化简与展示；ABI 插件。
import plugin:lib/plugin.mq.md
import sys:lib/sys.mq.md
---

## 确保插件

加载 ABI v2 `linalg` 插件一次。

*p = > plugin.native_path name="linalg"*
1. `p`
  > plugin.load path=`p`
2. *
  > print text=ext/linalg: 未找到原生 linalg 插件（请 build marqdo_plugin_linalg 或 marqdo ext add linalg）
  > sys.exit code=1
****

## 探测

> 确保插件
**> linalg_ping**

## 乘
    + `左`
    + `右`

矩阵乘（惰性；检查形状）。

> 确保插件
**> linalg_mul a=`左` b=`右`**

## 加
    + `左`
    + `右`

> 确保插件
**> linalg_add a=`左` b=`右`**

## 减
    + `左`
    + `右`

> 确保插件
**> linalg_sub a=`左` b=`右`**

## 转置
    + `式`

> 确保插件
**> linalg_transpose expr=`式`**

## 逆
    + `式`

> 确保插件
**> linalg_inv expr=`式`**

## 化简
    + `式`

应用矩阵表达式重写规则（R1–R5）。

> 确保插件
**> linalg_simplify expr=`式`**

## 文本
    + `式`

> 确保插件
**> linalg_ascii expr=`式`**

## 乳胶
    + `式`

> 确保插件
**> linalg_latex expr=`式`**

## 形状
    + `式`

> 确保插件
**> linalg_shape expr=`式`**

## 展示
    + `式`
    + `路径`=None

> 确保插件
**> linalg_show expr=`式` path=`路径`**

## 单位
    + `阶`

> 确保插件
**> linalg_eye n=`阶`**

## 零阵
    + `行`
    + `列`

> 确保插件
**> linalg_zeros rows=`行` cols=`列`**

## 自列表
    + `数据`

> 确保插件
**> linalg_from_list data=`数据`**

## 自公式
    + `公式`

吸收 `$$` 数值矩阵公式为稠密叶。

> 确保插件
**> linalg_from_formula formula=`公式`**

## 求值
    + `式`

将仅含稠密/单位/零阵运算的表达式求成 `linalg_dense`。

> 确保插件
**> linalg_explicit expr=`式`**

## 行列式
    + `式`

> 确保插件
**> linalg_det expr=`式`**

## 迹
    + `式`

> 确保插件
**> linalg_trace expr=`式`**

## 求解
    + `左`
    + `右`

解 `左 · x = 右`，返回 `linalg_solve`。

> 确保插件
**> linalg_solve a=`左` b=`右`**

## 稠密乘
    + `左`
    + `右`

> 确保插件
**> linalg_matmul a=`左` b=`右`**

## 符号
    + `名`
    + `行`
    + `列`

抽象矩阵符号（`linalg_expr`）。

> 确保插件
**> linalg_symbol name=`名` rows=`行` cols=`列`**

# 矩阵

## 符号
    + `名`
    + `行`
    + `列`

**> linalg_symbol name=`名` rows=`行` cols=`列`**

## 单位
    + `阶`

**> linalg_eye n=`阶`**

## 零阵
    + `行`
    + `列`

**> linalg_zeros rows=`行` cols=`列`**

## 自列表
    + `数据`

**> linalg_from_list data=`数据`**
