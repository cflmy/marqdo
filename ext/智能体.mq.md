---
title: ext/智能体
description: 智能体开发框架（布局 ABI + 匹配/派发）。可组合 ext/大模型。
> lib/插件.mq.md
> ext/大模型.mq.md
---

## 加载原生

加载 `plugins/agent`。顺序：`MARQDO_AGENT_PLUGIN`，否则 `marqdo ext add agent`。

*`环境路径` = > 取环境 名=MARQDO_AGENT_PLUGIN *
+ `环境路径`
  **> 加载 路径=`环境路径`**
+ *
  *`原生` = > host_ext_native_path name=agent *
  + `原生`
    **> 加载 路径=`原生`**
  + *
    > 打印 内容=ext/智能体: 请设置 MARQDO_AGENT_PLUGIN 或执行 marqdo ext add agent
    > 退出 码=1

# 智能体
    - 根

+ `根`
  *`r` = `根`*
+ *
  *`起点` = > 工作目录 *
  *`r` = > agent_find_root start=`起点` markers=agents,runbooks,marqdo.agent.json *

*`t` = > parse text={"p":"{\"_type\":\"智能体\",\"root\":","s":"}"} *
*`qr` = > quote text=`r` *
*`p` = > get value=`t` key=p *
*`s` = > get value=`t` key=s *
*`raw` = `p` + `qr` + `s` *
**> parse text=`raw`**

## 探测

*`root` = > get value=`自` key=root *
**> agent_probe root=`root`**

## 确保布局

*`root` = > get value=`自` key=root *
**> agent_ensure_layout root=`root`**

## 脚手架
    - 名
    - 模板
    - 目标

*`root` = > get value=`自` key=root *
**> agent_scaffold root=`root` name=`名` template=`模板` dest=`目标`**

## 技能匹配
    - 技能
    - 成员表

**> agent_match_skill skill=`技能` members=`成员表`**

## 分配任务
    - 任务ID
    - 分配给

*`t` = > parse text={"p":"{\"success\":true,\"task_id\":","m":",\"assignee\":","s":"}"} *
*`qid` = > quote text=`任务ID` *
*`qa` = > stringify value=`分配给` *
*`p` = > get value=`t` key=p *
*`m` = > get value=`t` key=m *
*`s` = > get value=`t` key=s *
*`raw` = `p` + `qid` + `m` + `qa` + `s` *
**> parse text=`raw`**

## 更新负载
    - 成员
    - 变化

**> agent_bump_load member=`成员` delta=`变化`**

## 创建工单
    - 标题
    - 详情

*`_n` = > `自`.确保布局 *
*`body` = `标题` *
*`out` = ticket.txt *
> host_write_text path=ticket.txt text=`body`
**`out`**

## 通知
    - 收件人
    - 内容

> 打印 内容=notify
> 打印 内容=`收件人`
> 打印 内容=`内容`
****

## 起草消息
    - 模型
    - 提示

**> `模型`.运行 提示=`提示`**
