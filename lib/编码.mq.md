---
title: lib/编码
description: Base64、十六进制与基32编解码（中层 M1 + Mid2 M8）。UTF-8 文本往返。
---

## 基64编码
    + `内容`

**> host_encoding_base64_encode text=`内容`**

## 基64解码
    + `内容`

**> host_encoding_base64_decode text=`内容`**

## 十六进制编码
    + `内容`

**> host_encoding_hex_encode text=`内容`**

## 十六进制解码
    + `内容`

**> host_encoding_hex_decode text=`内容`**

## 基32编码
    + `内容`

**> host_encoding_base32_encode text=`内容`**

## 基32解码
    + `内容`

**> host_encoding_base32_decode text=`内容`**
