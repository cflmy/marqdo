---
title: lib/日志
description: 按级别过滤的一行日志（中层 M6 + Mid2 M10 字段）。默认级别 info。
---

## 设级别
    + `级别`

级别：debug / info / warn / error（或 调试/信息/警告/错误）。

**> host_log_set_level level=`级别`**

## 调试
    + `内容`
    + `字段`=None

*line = > host_log_line level="debug" text=`内容` fields=`字段`*
1. `line`
  > 打印 内容=`line`
2. *
  ****

## 信息
    + `内容`
    + `字段`=None

*line = > host_log_line level="info" text=`内容` fields=`字段`*
1. `line`
  > 打印 内容=`line`
2. *
  ****

## 警告
    + `内容`
    + `字段`=None

*line = > host_log_line level="warn" text=`内容` fields=`字段`*
1. `line`
  > 打印 内容=`line`
2. *
  ****

## 错误
    + `内容`
    + `字段`=None

*line = > host_log_line level="error" text=`内容` fields=`字段`*
1. `line`
  > 打印 内容=`line`
2. *
  ****
