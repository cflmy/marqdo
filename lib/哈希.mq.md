---
title: lib/哈希
description: 摘要与 HMAC（中层 M3）。完整性 / 缓存键 — 非密码存储。
---

## 哈希256
    + `内容`

**> host_hash_sha256 text=`内容`**

## 哈希1
    + `内容`

**> host_hash_sha1 text=`内容`**

## md5
    + `内容`

**> host_hash_md5 text=`内容`**

## 密钥哈希256
    + `密钥`
    + `内容`

**> host_hash_hmac_sha256 key=`密钥` text=`内容`**
