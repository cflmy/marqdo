---
title: lib/配置表
description: 只读 TOML 子集解析（中层 Mid2 M8）。表 / 标量 / 数组；无数组表。
---

只读 TOML 子集。适合配置；不做完整 TOML 写出。

## 解析
    + `内容`

把 TOML 文本解析成映射/列表/标量。

调用例：[配置表.解析] 内容=`配置`

*> host_toml_parse text=`内容`*
