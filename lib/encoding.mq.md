---
title: lib/encoding
description: Base64 and hex encode/decode (Mid M1). UTF-8 text round-trip.
---

## base64_encode
    + `text`

**> host_encoding_base64_encode text=`text`**

## base64_decode
    + `text`

**> host_encoding_base64_decode text=`text`**

## hex_encode
    + `text`

**> host_encoding_hex_encode text=`text`**

## hex_decode
    + `text`

**> host_encoding_hex_decode text=`text`**
