---
title: lib/时间
description: 中文时间包装
---

## 此刻秒

**> host_now_unix**

## 此刻毫秒

**> host_now_ms**

## 格式化
    + `秒`
    + `格式`

**> host_format_time unix=`秒` pattern=`格式`**

## 睡眠毫秒
    + `毫秒`

**> host_sleep_ms ms=`毫秒`**

## 解析时间
    + `内容`
    + `格式`

**> host_parse_time text=`内容` pattern=`格式`**
