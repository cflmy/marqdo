---
title: lib/子任务
description: OS 子进程子任务（父进程结束则子进程结束）
---

## 启动
    + `路径`

**> host_subtask_spawn path=`路径`**

## 探测
    + `id`

**> host_subtask_poll id=`id`**

## 等待
    + `id`

**> host_subtask_join id=`id`**

## 终止
    + `id`

**> host_subtask_kill id=`id`**

## 等待全部

**> host_subtask_wait_all**
