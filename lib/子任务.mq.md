---
title: lib/子任务
description: 并发子任务 — 文件、函数或外联代码
---

## 启动
    + `路径`=None
    + `函数`=None
    + `参数`=None
    + `代码`=None
    + `语言`=None
    + `源码`=None
    + `输入`=None

**> host_subtask_spawn path=`路径` fn=`函数` args=`参数` code=`代码` lang=`语言` source=`源码` stdin=`输入`**

## 探测
    + `id`

**> host_subtask_poll id=`id`**

## 等待
    + `id`

**> host_subtask_join id=`id`**

## 终止
    + `id`

**> host_subtask_kill id=`id`**

## 等待全部

**> host_subtask_wait_all**
