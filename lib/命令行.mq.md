---
title: lib/命令行
description: 解析命令行参数（中层 M5）。--键 值 / --键=值 / --开关；位置参在 _。
---

把进程参数解析成映射。开关与位置参（落在 _）。

## 解析
    + `参数`=None

解析 --键 值 / --键=值 / --开关。裸 --开关 若当布尔，后面不要紧跟非选项词（开关放最后，或写 --开关=true）。位置参进 _。省略参数时用进程 argv（系统.参数表）。

调用例：[命令行.解析]

*> host_cli_parse args=`参数`*
