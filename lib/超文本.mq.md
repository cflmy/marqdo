---
title: lib/超文本
description: HTML 转义 / 还原（中层 Mid2 M8）。拼装字符串时的 XSS 底线。
---

HTML 转义助手。把不可信文本拼进标记前先转义。

## 转义
    + `内容`

对内容做 HTML 转义。

调用例：[超文本.转义] 内容=`段`

*> host_html_escape text=`内容`*

## 还原
    + `内容`

还原内容里的 HTML 实体。

*> host_html_unescape text=`内容`*
