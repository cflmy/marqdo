---
title: lib/网络
description: 中文 HTTP(S) 包装。可选 headers / content_type 经宿主别名绑定。
---

## 获取
    - 地址

支持 HTTPS。可选具名实参 `headers=`（映射）。

**> host_http_get url=`地址`**

## 提交
    - 地址
    - 内容

默认 Content-Type 为 JSON。可选 `content_type=` / `headers=`。

**> host_http_post url=`地址` body=`内容`**

## 请求
    - 方法
    - 地址

可选 `body=` / `content_type=` / `headers=`。

**> host_http_request method=`方法` url=`地址`**

## 编码地址
    - 内容

**> host_url_encode text=`内容`**
