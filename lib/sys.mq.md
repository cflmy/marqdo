---
title: lib/sys
description: English system wrappers
---

# env_get
    - name

**> host_env_get name=`name`**

# env_set
    - name
    - value

**> host_env_set name=`name` value=`value`**

# args

**> host_args**

# cwd

**> host_cwd**

# exit
    - code

**> host_exit code=`code`**

# exec
    - cmd

**> host_exec cmd=`cmd`**
