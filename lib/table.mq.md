---
title: lib/table
description: English list helpers over len / at (prefer footnote index for one-shots)
---

## rows
    + `xs`

**> len `xs`**

## row_at
    + `xs`
    + `i`

**> at value=`xs` index=`i`**
