# quantum-bell

Bell |Φ⁺⟩ via `ext/quantum` circuit table + draw.

```bash
cargo build -p marqdo_plugin_quantum
marqdo run examples/quantum-bell/index.mq.md
```

Writes `bell.svg` next to the program (sandbox cwd).
