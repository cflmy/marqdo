---
title: 中层标准库 — 哈希与机密
description: lib/哈希 + lib/机密，无需插件（中层 M3）
导入 哈希:lib/哈希.mq.md
导入 机密:lib/机密.mq.md
---

# 摘要与安全令牌

完整性摘要与加密令牌进 `lib/` — 勿用 `math.random` 生成令牌。

# main

*`d` = > 哈希.哈希256 内容="marqdo"*
*`t` = > 机密.十六进制令牌 字节数=8*
*`L` = > 长度 `t`*
> 打印 内容=`d`
> 打印 内容=`L`
1. `L` == 16
  > 打印 内容=mid-m3-ok
2. *
  > 打印 内容=mid-m3-fail
