---
title: 字节码后端
description: `--backend bytecode` 与树遍历共用语义
---

# main

里程碑：字节码原型。

同一套 `.mq.md` 可用树遍历或字节码 VM：

  marqdo run FILE --backend tree

  marqdo run FILE --backend bytecode

两种后端输出一致：

> 打印 内容=字节码后端：同一程序，另一引擎。
