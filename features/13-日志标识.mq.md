---
title: 中层标准库 — 日志与标识
description: 按级别过滤的日志 + UUID 版本4（中层 M6）
导入 日志:lib/日志.mq.md
导入 标识:lib/标识.mq.md
---

# 无插件的可观测脚本

默认日志级别为 info；需要细节时再调到 debug。用 标识.版本4 生成 id。

# main

> 日志.信息 内容="中层-m6-开始"

> 日志.调试 内容="默认会跳过"

> 日志.设级别 级别="debug"

> 日志.调试 内容="现在可见"

*id = > 标识.版本4*
*n = > 长度 `id`*
> 打印 内容=`n`
