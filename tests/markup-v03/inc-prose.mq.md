---
title: v0.3 加一（叙述统一动机例）
description: 叙述声明 `n`；粗体赋值；斜体返回；`>` 调用后打印返回值
markup: v0.3
---

## 加一函数

对于输入变量`n`,我们执行操作**n=n+1**接着返回*n*。

# main

我要调用加一函数可以采用这样的办法：

**结果 = > 加一函数 37**
**print text=结果**
