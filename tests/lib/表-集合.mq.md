---
title: 表集合
description: 改 + 列表字典
> lib/表.mq.md
---

# main

*`h` = > 表.改 入=None 于=Authorization 值=Bearer-x*
> 打印 内容=`h`[^Authorization]

`xs` =

| v |
|---|
| a |
| b |

*`xs` = > 表.改 入=`xs` 于=1 值=A*
> 打印 内容=`xs`[^1]

*`xs` = > 表.追加 list=`xs` item=c*
*`n` = > 表.长度 xs=`xs`*
> 打印 内容=`n`
