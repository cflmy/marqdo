---
title: map footnote digit keys
description: Pure-digit labels are map keys (text), not list indices. Bare base ok.
---

# main

`p` =

| 00 | 11 |
|----|----|
| half | other |

*`a` = p[^00] *
*`b` = `p`[^11] *

> print text=`a`
> print text=`b`
